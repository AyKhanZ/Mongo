export type Product = {
    id: number;
    id1C: string;
    name: string;
    desc: string;
    img: string;
    productType: string;
};

export type News = {
    id: string;
    title: string;
    description: string;
    img?: string;
    imageFile?: File;
};
